use crate::other::data::AircraftData;

pub(crate) mod progress;
pub(crate) mod altitude;
pub(crate) mod full;

pub(crate) fn progress(plane_data: Option<AircraftData>) {
    todo!(
        "call progress::regular"
    )
}

pub(crate) fn altitude(plane_data: Option<AircraftData>) {
    todo!(
        "call altitude::regular"
    )
}

pub(crate) fn full(plane_data: Option<AircraftData>) {
    todo!(
        "call full::regular"
    )
}


// once
pub(crate) fn progress_once(plane_data: Option<AircraftData>) {
    todo!(
        "call progress::once"
    )
}

pub(crate) fn altitude_once(plane_data: Option<AircraftData>) {
    todo!(
        "call altitude::once"
    )
}

pub(crate) fn full_once(plane_data: Option<AircraftData>) {
    todo!(
        "call full::once"
    )
}
